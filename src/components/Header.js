import React from 'react'
import { AppBar, Toolbar } from '@mui/material'

const Header = () => {
  return (
      <div>
          <AppBar >
              <Toolbar>
                  
              </Toolbar>
          </AppBar>
    </div>
  )
}

export default Header